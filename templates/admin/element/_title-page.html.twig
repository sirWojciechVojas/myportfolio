<header class="mb-3 mb-md-5">
    <h1>{{ title }}</h1>
    {% if path is defined %}
        <a class="btn btn-sm btn-primary text-uppercase" href="{{ path(path) }}">ajouter</a>
    {% endif %}
</header>